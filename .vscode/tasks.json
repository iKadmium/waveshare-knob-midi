{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "PlatformIO: Build (native)",
            "type": "shell",
            "command": "pio run -e native",
            "options": {
                "cwd": "${workspaceFolder}/esp32"
            },
            "group": {
                "kind": "build",
                "isDefault": false
            },
            "problemMatcher": [
                "$gcc"
            ]
        },
        {
            "label": "PlatformIO: Upload and Pre-Debug (esp32-s3-idf)",
            "type": "shell",
            "command": "pio run -e esp32-s3-idf -t upload",
            "options": {
                "cwd": "${workspaceFolder}/esp32"
            },
            "problemMatcher": [
                "$platformio"
            ]
        }
    ]
}